// Client-side plugin to ensure authentication works properly
export default defineNuxtPlugin(() => {
  // This ensures that cookies are available for client-side navigation
  // No special handling needed - cookies are automatically sent
});